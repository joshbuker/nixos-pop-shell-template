# Zsh
## Powerlevel10k

I have powerlevel10k preconfigured with my preferred options to give you a nice default. However you can replace `.p10k.zsh` with your own preferences by running `p10k configure` and using what it generates instead.
